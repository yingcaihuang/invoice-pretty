# HTTPS Production Environment Variables for Web Invoice Processor

# Domain Configuration (REQUIRED)
DOMAIN=your-domain.com
TRAEFIK_DOMAIN=traefik.your-domain.com

# Port Configuration (Traefik handles these automatically)
# - Port 80: HTTP (auto-redirects to HTTPS)
# - Port 443: HTTPS (main application traffic)
# - No additional ports exposed for security

# ACME/Let's Encrypt Configuration (REQUIRED)
ACME_EMAIL=your-email@example.com
# Use staging server for testing: https://acme-staging-v02.api.letsencrypt.org/directory
# Use production server: https://acme-v02.api.letsencrypt.org/directory (default)
ACME_CA_SERVER=https://acme-v02.api.letsencrypt.org/directory

# Traefik Dashboard Authentication (REQUIRED)
# Generate with: echo $(htpasswd -nb admin your-password) | sed -e s/\\$/\\$\\$/g
# Example: admin:$$2y$$10$$...
TRAEFIK_AUTH=admin:$$2y$$10$$rO7VlFuBXF.LQPqHpvUGOOLUOvkiQp7VF8ashLgjU7dqXFXCrRTS2

# Application Configuration
DEBUG=false
MAX_FILE_SIZE=52428800
CLEANUP_INTERVAL=24
MAX_CONCURRENT_TASKS=4

# Logging
TRAEFIK_LOG_LEVEL=INFO

# Optional: Custom Redis Configuration
# REDIS_PASSWORD=your_redis_password
# REDIS_DB=0

# Optional: Security Configuration
# SECRET_KEY=your_secret_key_here
